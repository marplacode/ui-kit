import { Box } from "@chakra-ui/react";
import { motion, useInView } from "framer-motion";
import { FC, useLayoutEffect, useMemo, useRef, useState } from "react";


/**
 * ReadUser Use Case Class
 * @abstract
 * The ReadUser class is a use case in the application layer of our clean architecture design.
 * It represents the action of reading a user's details from a repository using a given identifier.
 * This class is initialized with a userRepository and a locationRepository which are leveraged to read user details during the execution of the run method as well as
 * the user location.
 *
 * @class
 */
export const Animation: FC<any> = ({}) => {
  return <Box></Box>;
};
